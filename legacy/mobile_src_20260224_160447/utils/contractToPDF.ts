/**
 * Stub: convert a rendered contract string into a PDF.
 *
 * Future implementations could:
 * - Render HTML into a WebView and print to PDF
 * - Use a native module / server-side PDF renderer
 * - Upload to storage and return a URL
 */
export async function contractToPDF(_renderedContract: string): Promise<string | null> {
  // TODO: Implement PDF generation + upload.
  return null;
}
